function sayHello() {
  alert("This is testing for javascript");
}

function getSum(first, second) {
  var sum = first + second;
  alert("The sum of " + first + " and " + second + " is " + sum);
}

function total() {
  let first = 5;
  let second = 5;
  getSum(first, second);
}
